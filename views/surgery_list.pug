extends layout

block content
  h1= title
  
  ul
  each surgery in surgery_list
    li 
      a(href=surgery.url) #{surgery.doctor.name} - #{surgery.patient.name}

  else
    li There are no surgeries.
